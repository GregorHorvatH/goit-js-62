import { Timer } from './Timer';

const timer1 = new Timer({ selector: '#timer-1', value: 10 });
const timer2 = new Timer({ selector: '#timer-2', value: 20 });
const timer3 = new Timer({ selector: '#timer-3', value: 30 });
